# Tasks

- [ ] Add the tray menu and wire the primary shell actions
- [ ] Implement loading and startup-failure states around the webview
- [ ] Hook documentation and config-folder actions to the packaged paths
- [ ] Add restart-server behavior that cooperates with the sidecar lifecycle
- [ ] Validate shell controls against the packaged runtime layout
