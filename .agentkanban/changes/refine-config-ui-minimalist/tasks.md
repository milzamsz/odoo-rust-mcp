# Tasks

- [x] Create the new spec-driven task record for the minimalist Config UI refinement and move it to `in-progress`
- [x] Update the shared Config UI experience spec to match the new light-first sidebar-and-drawer behavior
- [x] Replace the global theme, typography, icons, and shell styling with the minimalist visual system
- [x] Add a persistent collapsible desktop sidebar and quieter header/status treatment
- [x] Rewrite the instances workspace to feel cleaner in both card and table modes
- [x] Move the instance editor into a right-side drawer with sticky actions
- [x] Move the prompt editor into a right-side drawer and align it with the same form language
- [x] Refine overview, login, tools, server, and security screens to use the same cleaner system
- [x] Unify typography weights and add a persisted `Light / Dark / Auto` appearance selector with
  `Auto` as the default
- [x] Update `TECHNICAL.md`, `.agentkanban/memory.md`, and relevant user docs for the new UI behavior
- [x] Run `npm run lint`, `npm run typecheck`, `npm test -- --run`, and `npm run build`
- [x] Add the Rust Hexagon application mark and apply it to the Mantine shell and browser metadata
- [x] Keep the Documentation sidebar action accessible and verify `/docs/` behavior
- [x] Package mdBook output and the Windows icon in release/install flows
- [x] Validate UI, mdBook, Rust docs discovery, and Windows shortcut icon behavior
