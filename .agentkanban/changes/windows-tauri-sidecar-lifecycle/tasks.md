# Tasks

- [ ] Bundle `rust-mcp.exe` as a Tauri sidecar
- [ ] Start the sidecar with the current HTTP + Config UI ports
- [ ] Gate window readiness on the local Config UI health check
- [ ] Add deterministic sidecar log capture for startup and shutdown diagnostics
- [ ] Validate quit behavior only stops the app-owned child process
