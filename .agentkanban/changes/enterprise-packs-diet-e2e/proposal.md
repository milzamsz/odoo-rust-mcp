# Proposal

Reuse the existing declarative registry and Odoo client pool. Add only the cache and metadata needed to hide tools that cannot work on a selected instance. Keep workflow engines, YAML pack manifests, exposure profiles, domain tool suites, and new token systems out of scope.

