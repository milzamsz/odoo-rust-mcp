# Phase 07 Prompt: Workflow Runtime

Implement deterministic workflow definitions and file-backed manifests.

Required:

- dry-run;
- step persistence;
- idempotency keys;
- resume;
- state reconciliation;
- explicit compensation metadata;
- workflow resources and tools;
- process-restart tests.

Do not add Redis, a broker, or distributed workers.
