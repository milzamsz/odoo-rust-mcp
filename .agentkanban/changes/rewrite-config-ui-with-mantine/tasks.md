# Tasks

- [x] Create the new spec-driven task record for the Mantine rewrite and move it to `in-progress`
- [x] Upgrade `config-ui` dependencies to React 19, Mantine 9, routing, table state, and Playwright support
- [x] Replace the existing custom shell with a Mantine `AppShell` and hash-route navigation
- [x] Add the Operations Overview landing page
- [x] Rewrite the instances experience with Mantine card/table views, modal editing, and column filtering
- [x] Rewrite tools, prompts, server, and security screens using Mantine patterns
- [x] Remove Tailwind from the runtime UI and clean up obsolete frontend components
- [x] Bump the repo version to `0.4.0` in both `config-ui/package.json` and `rust-mcp/Cargo.toml`
- [x] Update `README.md`, `AGENTS.md`, and `TECHNICAL.md` to reflect the rewritten UI stack and routing model
- [x] Rebuild embedded assets, run validation, and attempt an independent implementation review (review agents timed out without findings)
