# Phase 02 Prompt: Capability Engine

Implement per-instance capability snapshots.

Required outcomes:

- identity and Odoo version;
- installed module discovery;
- model and field metadata;
- access checks;
- JSON persistence;
- refresh command;
- stale snapshot handling;
- MCP resources and tools;
- fixtures and tests.

Do not perform unsafe create/delete probes. Do not change production tool
exposure until capability accuracy is tested.
