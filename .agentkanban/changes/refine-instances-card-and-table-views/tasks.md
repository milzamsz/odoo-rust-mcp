# Tasks

- [x] Re-read `.agentkanban/INSTRUCTION.md` and `.agentkanban/memory.md` before implementation starts
- [x] Review `config-ui/src/components/tabs/InstancesTab.tsx`, `config-ui/src/types.ts`, and `config-ui/src/__tests__/instances-tab.test.tsx`
- [x] Add shared normalized row metadata plus combined filter and sort helpers for the instances list
- [x] Add desktop Card/Table view switching with persisted desktop preference and narrow-screen card fallback
- [x] Implement table rendering with the required columns, typed column filters, and sortable headers
- [x] Preserve existing card behavior and shared page-level actions across both views
- [x] Add active-filter count, removable filter chips or equivalent visible state, and clear-all behavior
- [x] Expand UI tests for view switching, persistence, responsiveness, filtering, sorting, and existing actions
- [x] Run `cd config-ui && npm run lint`
- [x] Run `cd config-ui && npm run typecheck`
- [x] Run `cd config-ui && npm test`
- [x] Run `cd config-ui && npm run build`
